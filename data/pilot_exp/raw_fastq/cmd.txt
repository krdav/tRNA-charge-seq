
# Convert .gz to bz2 compression, dryrun:
parallel --dry-run 'zcat {} | bzip2 > {.}.bz2' ::: *.gz

# Perform the actual conversion:
parallel --bar 'zcat < {} | bzip2 > {.}.bz2' ::: *.gz

# Delete old .gz files:
rm *.gz

